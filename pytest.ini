[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    smoke: quick, environment-light checks
    integration: runs against external services (e.g., OpenAI)
    browserbase: runs against a remote CDP provider (e.g., Browserbase)
